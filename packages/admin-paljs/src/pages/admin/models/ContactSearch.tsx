import React from 'react';
import PrismaTable from 'components/PrismaTable';

const ContactSearch: React.FC = () => {
  return <PrismaTable model="ContactSearch" />;
};

export default ContactSearch;
