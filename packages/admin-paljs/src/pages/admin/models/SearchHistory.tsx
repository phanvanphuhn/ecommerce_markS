import React from 'react';
import PrismaTable from 'components/PrismaTable';

const SearchHistory: React.FC = () => {
  return <PrismaTable model="SearchHistory" />;
};

export default SearchHistory;
